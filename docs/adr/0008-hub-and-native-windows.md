# Hub plus one native window per log source

The shell is a Hub and one ordinary OS window per log source. No tabs, no frameless chrome, no magnetic snap. Those were a later skin on Ouroboros-old, not the domain.

**Considered:** full old chrome (frameless + magnetic); tabs in one window (superseded in the old app).
