# Static analysis is the primary workflow

Opened log sources start with follow mode off. Tail is opt-in per window. Live-first would change rotation, the rolling window, and every open path; the old app already tails when asked.

**Considered:** follow on by default; choose Analyze vs Follow at open.
